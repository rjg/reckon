{
  "name": "reckon",
  "version": "1.0.0",
  "description": "Reckon mental-math PWA — pure logic is unit-tested with node:test (no dependencies).",
  "private": true,
  "scripts": {
    "test": "node --test",
    "dev": "node dev.js",
    "dev:lan": "node dev.js --lan"
  }
}
